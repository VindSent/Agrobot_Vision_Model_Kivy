#:kivy 2.3.1

<MainScreen>:
    BoxLayout:
        orientation: "vertical"
        counter_widget: counter_widget 
        CounterWidget:
            id: counter_widget
        # Button:
        #     text: "Go to Settings"
        #     on_release: app.root.current = "settings"
